export enum OrderPriority {
  LOW = 1,
  HIGH = 50,
}

export enum OrderStatus {
  PENDING = 'pending',
  COMPLETED = 'completed',
}

export enum BotStatus {
  IDLE = 'idle',
  PROCESSING = 'processing',
}
